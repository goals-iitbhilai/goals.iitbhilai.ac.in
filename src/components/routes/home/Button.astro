---
interface Props {
  href?: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a
  {href}
  class={`px-5 py-3 rounded-md backdrop-blur-2xl bg-white/25 hover:bg-white/35 transition-all hover:border-white/75 border border-white/50  ${className}`}
>
  <slot />
</a>
